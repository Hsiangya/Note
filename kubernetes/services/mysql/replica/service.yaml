apiVersion: v1
kind: Service
metadata:
  name: mysql-replica
  labels:
    app: mysql
  namespace: dev
spec:
  ports:
  - name: mysql
    port: 3306
  clusterIP: None
  selector:
    app: mysql
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-read
  labels:
    app: mysql
  namespace: dev
spec:
  ports:
  - name: mysql
    port: 3306
  selector:
    app: mysql
